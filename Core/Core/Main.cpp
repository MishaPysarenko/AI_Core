#include "Model.h"
#include <fstream>
#include <iostream>
void main() 
{
	/*std::fstream file("file.txt", std::ios::in);
	std::string line;
	while (std::getline(file, line))
	{
		std::cout << line << '\n';
	}*/
}